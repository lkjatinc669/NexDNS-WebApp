{
  "name": "nexdns-webapp",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for NexDNS WebApp: React frontend + Express backend + DNS server",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {},
  "keywords": [
    "monorepo",
    "dns",
    "local-network",
    "react",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT"
}
